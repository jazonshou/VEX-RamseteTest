#pragma once

class Math {
    public: 
    static double tickToFt(double ticks);
    static double rpmToFtps(double rpm);
    static double ftToMeters(double ft);
    static double metersToFt(double meters);
};